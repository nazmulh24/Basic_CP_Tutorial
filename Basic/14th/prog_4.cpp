//--> Supercentral Point

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, count = 0;
    cin >> n;
    while (n--)
    {
        int ar[2];
        for (int i = 0; i < 2; i++)
            cin >> ar[i];
        for (int i = 0; i < 2; i++)
        {
            abs(a[i]);
            if ((ar[0] == ar[1]) || (ar[0] >= 1 && (ar[1] == ar[0] + 1)))

                count++;
        }
    }
    cout << count << endl;
    return 0;
}
